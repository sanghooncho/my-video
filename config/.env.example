# DO NOT COMMIT real credentials here.

# Reuse the same Google service account JSON used by marketing-team.
# Point this to the file path on the server.
GOOGLE_APPLICATION_CREDENTIALS=/home/opc/.openclaw/workspace/projects/preview/marketing-team/config/gcs-credentials.json
